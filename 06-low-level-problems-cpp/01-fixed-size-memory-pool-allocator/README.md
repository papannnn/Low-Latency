# Implement a Fixed-Size Memory Pool Allocator

## Problem

Design and implement a fixed-size memory pool that pre-allocates a large chunk of memory and serves fixed-size object allocations in O(1) time.

## Requirements

- Avoid fragmentation.
- Support deallocation.
- Discuss cache locality.
- Discuss alignment concerns.
