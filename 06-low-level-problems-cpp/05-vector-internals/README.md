# Implement `std::vector` Internals

## Problem

Design a simplified `Vector<T>` class that manages dynamic memory.

## Requirements

- Support `push_back`.
- Support `reserve`.
- Support `resize`.
- Support copy and move semantics.
- Provide strong exception safety guarantees.
