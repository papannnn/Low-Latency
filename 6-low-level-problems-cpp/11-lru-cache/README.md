# Build a LRU Cache

## Problem

Design a thread-safe LRU cache with O(1) get and put operations using a combination of hash map and doubly linked list.

## Requirements

- Provide O(1) `get`.
- Provide O(1) `put`.
- Ensure correctness under concurrent access.
