# Implement a Variant Type

## Problem

Design a type-safe union that can hold one of several types.

## Requirements

- Store one active type at a time.
- Manage object lifetime safely.
- Implement visitation logic similar to `std::variant`.
