# Abstract Factory Pattern

## Problems

- Design an exchange connectivity module that creates families of related objects, such as `OrderSender` and `MarketDataSubscriber`, for different exchanges like CME or NASDAQ.
- Design a UI toolkit that generates platform-specific components without coupling to concrete classes.
