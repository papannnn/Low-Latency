# Platform-Specific / Low-Level Timers

## Topics

- `setitimer()` and `timerfd_create()` on Linux C APIs
- `GetTickCount()` and `QueryPerformanceCounter()` on Windows
- Real-time timer handling in embedded systems, such as Arduino and STM32
